This repo is for all the certification tasks that I will do through the FreeCodeCamp certification. 

The first is the universe.sql file: This is a PostgreSQL database created in the Bash terminal. It has SQL queries like CREATE, JOINS, SELECT, DROP, ALTER, and INSERT.
It also includes constraints such as UNIQUE, FOREIGN KEY, PRIMARY KEY, NULL, VARCHAR, etc.
